import * as ActionType from 'actions/index'
import Immutable from 'immutable'

let defaultState = Immutable.fromJS([])

function firebaseReducer (state = defaultState, action) {

}

export default firebaseReducer
